const Config = {
  hostname: '<%= mqttHostName %>',
  port: '<%= mqttPort %>',
  prefix: '<%= mqttPrefix %>',
  // subTopic: '<%= mqttPrefix + mqttDeviceName %>/status',
  // pubTopic: '<%= mqttPrefix + mqttDeviceName %>/$/command',
  clientId: '<%= mqttClientId %>'
}
